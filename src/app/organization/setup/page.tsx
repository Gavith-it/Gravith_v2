'use client';

import { OrganizationSetup } from '@/components/OrganizationSetup';

export default function OrganizationSetupPage() {
  return <OrganizationSetup onComplete={() => {}} onBack={() => {}} />;
}
