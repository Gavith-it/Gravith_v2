'use client';

import { WorkProgressPage } from '@/components/work-progress';

export default function WorkProgressPageRoute() {
  return <WorkProgressPage selectedSite="1" onSiteSelect={() => {}} />;
}
